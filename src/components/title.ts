import styled from "styled-components";

export const Title = styled.h1`
  font-size: 3rem;
  margin: 0;
  z-index: 1;
`;

export const Subtitle = styled.h2`
  font-size: 1.5rem;
  font-weight: 300;
  z-index: 1;
  margin: 0;
`;
